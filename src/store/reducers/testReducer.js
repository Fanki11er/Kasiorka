const initialState = {
  test: null,
};

const testReducer = (state = initialState, actions) => {
  switch (actions.type) {
    default: {
      return state;
    }
  }
};

export default testReducer;
